#include <stdio.h>
#include <locale.h>
#include <stdlib.h>

int main()
{
    setlocale(LC_ALL, "Rus");
    int A,B,C;
    printf("Введите число A : ");
    scanf("%d",&A);
    printf("Введите число B : ");
    scanf("%d",&B);
    printf("Введите число C : ");
    scanf("%d",&C);
     
     if (abs(A-B)<abs(A-C))
      printf("Точка B лежит ближе");
      else
      printf("Точка C лежит ближе");
      
 
      
     
}
