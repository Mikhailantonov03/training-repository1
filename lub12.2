#include <stdio.h>
#include <locale.h>
#include <math.h>

int main()
{
	setlocale(LC_ALL, "RUS");

	float a;
	printf("Введите цену за килограмм:");
	scanf("%f",&a);

	for (float i = 0.1; i < 1.1; i += 0.1)
	{
		printf("Цена за %f",i); 
		printf(" кг конфет = %f",a*i);
		printf(" (руб) \n       ");
	}
}
