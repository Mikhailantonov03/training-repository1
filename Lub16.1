#include <stdio.h>
#include <locale.h>
#include <math.h>
int main(void)
{
 setlocale(LC_ALL, "RUS");
 int a[10];
 int n, i=1,j=1;
 printf("Введите N: ");
 scanf("%i",&n);
 for (i=0; i<n; ++i, j+=2) a[i]=j;
 for (i=0; i<n; ++i) printf("%i : %i\n",i,a[i]);
 return 0;
}
