#include <stdio.h>
#include <locale.h>

int main()
{
    setlocale(LC_ALL, "Rus");
    int A;
    printf("Введите число  : ");
    scanf("%d",&A);
    if ((A%1000)/100 < (A%100)/10 && (A%100)/10<A%10)
    printf("Цифры в числе имеют возрастающую последовательность");
    else if ((A%1000)/100 > (A%100)/10 && (A%100)/10>A%10)
    printf("Цифры в числе имеют убывающую последовательность");
 else 
 printf("Цифры в числе не имеют ни возрастающую ни убывающую последовательность");
}
