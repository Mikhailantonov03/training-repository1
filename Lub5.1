	#include <stdio.h>
	#include <stdlib.h>
	int main() {
	float x1, x2, y1, y2,gyp;
	system("chcp 1251");
	system("cls");
	printf("Введите координаты: ");
	

	printf("X1 : ");
	scanf("%f", &x1);
	printf("X2 : ");
	scanf("%f", &x2);
	printf("y1 : ");
	scanf("%f", &y1);
	printf("y2 : ");
	scanf("%f", &y2);
	if ((x1==x2) && (y1==y2)) {
	    printf("Лежат на одних кординатах");
	}
	else if (y1==y2){
	    gyp=abs(x1-x2);
	    printf("Расстояние равно %f", gyp);
	}
	else if (x1==x2){
	    gyp=abs(y1-y2);
	    printf("Расстояние равно %f",gyp);
	}
	else {
	    gyp=sqrt(abs(x1 - x2)*abs(x1 - x2)+abs(y1 - y2)*abs(y1 - y2));
	    printf("Расстояние равно %f",gyp);
	}
	return 0;
	}

