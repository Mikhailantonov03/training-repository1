#include <stdio.h>
#include <locale.h>

int main()
{
    
setlocale(LC_ALL, "Rus");
    float a,b,x;
    printf("Дано линейное уравнение : a*x+b=0 ");
     printf("Введите коэфф a : ");
    scanf("%f",&a);
    printf("Введите коэфф b : ");
    scanf("%f",&b);
    x=-b/a;
    printf("x равен : %f ",x);
    return 0;
}
