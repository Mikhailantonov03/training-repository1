#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
     int a,x,y;
     printf("Введите значение a: ");
     scanf("%d",&a);
   y=pow(a,3);
   x=y*y*y*y*y;
   printf("Ответ равен %d ",x);
    return 0;
}
