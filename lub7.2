#include <stdio.h>
#include <locale.h>

int main()
{
    
setlocale(LC_ALL, "Rus");
    float x;
     printf("Введите радианы : ");
    scanf("%f",&x);
    x=x*180;
    printf("Угол в градусах равен: %f градусов",x);
    return 0;
}
